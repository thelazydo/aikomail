DATABASE_URL=postgres://user:password@localhost:5432/email_verifier?sslmode=disable
PORT="8080"
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=email_verifier
SMTP_DOMAIN=localhost
SMTP_ADDRESS=example@gmail.com
RATE_LIMIT=5
WORKERS_COUNT=4
